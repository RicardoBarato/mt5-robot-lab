# Codex Assisted Mode

Use this skill for optional Codex workflows.

Rules:

- Codex is optional.
- Do not install Codex silently.
- Do not log in silently.
- Do not store tokens, passwords or credentials.
- Prepare mission packets only after explicit user consent.
- Record `codex_used` and `codex_authorized` in reports.
