# Windows Installer Strategy

Future installer:

```text
MT5-Robot-Lab-Setup.exe
```

The installer should be built with Inno Setup after the app is ready. It must not
bundle MT5, Codex, credentials, private reports or raw logs.
